version: "3.8"

services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot-data:/app/data
      - ./vacancy_monitor.session:/app/vacancy_monitor.session
    environment:
      - PYTHONUNBUFFERED=1
      - DB_PATH=data/bot.db

volumes:
  bot-data:
